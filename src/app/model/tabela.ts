/**
 * Objeto usado para exibir os dados dinamicamente na tabela.
 * Ex: [{ nomeColuna: "Nome", nomeValorColuna: "nome" }, { nomeColuna: "Descrição", nomeValorColuna: "descricao" }...]
 */
export class DadosTabela {
    nomeColuna: string;
    nomeValorColuna: any;
}